import GlobalStyles from "@mui/material/GlobalStyles";

export default function StylesGlobal() {
  return (
    <GlobalStyles
      styles={{
        a: {
          textDecoration: "none",
        },
        body: {
          background: "#ccc",
        },
      }}
    />
  );
}
